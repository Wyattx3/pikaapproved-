# Telegram session file as base64 string
# Generate with: python -c "import base64; print(base64.b64encode(open('pika_session.session','rb').read()).decode())"
SESSION_B64=<paste_base64_string_here>

# Optional: approval image as base64
# Generate with: python -c "import base64; print(base64.b64encode(open('image.png','rb').read()).decode())"
IMAGE_B64=<paste_base64_string_here>
